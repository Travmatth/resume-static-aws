/* @flow */
'use strict';

describe('Calculator Handlers', () => {
  beforeAll(() => {
    const file = require('../index.pug');
    console.log(file);
    //document.body.innerHTML = ``;
  });

  it('', () => {});
});
