doctype html

html
	head
		meta(charset='utf-8')
		meta(http-equiv='x-ua-compatible', content='ie=edge')
		meta(name='description' content='')
		meta(name='viewport' content='width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no')
		if (Array.isArray(webpack.assetsByChunkName.vendor))
			script(src=`${webpack.assetsByChunkName.vendor[0]}`)
		else
			script(src=`${webpack.assetsByChunkName.vendor}`)
		block heading
	body(ontouchstart='')
		block content