/* @flow */
import { render, update } from './Helpers';
import { createStartView, startButton, sideSelectionButton } from './Start';
import { createPlayView, gameTile, undoButton } from './Play';
import { createScoreView, scoreElement, lifecycleButton } from './Score';

export {
  render,
  update,
  createStartView,
  startButton,
  sideSelectionButton,
  createPlayView,
  gameTile,
  undoButton,
  createScoreView,
  scoreElement,
  lifecycleButton,
};
